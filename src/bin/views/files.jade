extends layout

block vars
    - var title = "Player two - Files"

block content
    h1 Files
    - for (var i in files) {
        a(href="/dl#{files[i].path}")
            img(src="#{files[i].path}")
    -}

block footer